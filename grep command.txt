Grep commands 
grep "inhibit" <annotation file name> > inhibit.txt
#Then for creating the id of inhibitor 
Before this copy only sequence id remove all other information (also remove >sign)
Then, type
vi id.txt and press inter
Then, 
press i to enter the copied sequences id's
Then, paste the sequence id in the command line
Then, 
hit ESC :wq!

#Then for chomping the sequence with the id, run the following pearl command 
perl -ne 'if(/^>(\S+)/){$c=$i{$1}}$c?print:chomp; $i{$_}=1 if @ARGV' id.txt GCF_019186545.1_ASM1918654v1_protein.faa > inhibit.fa